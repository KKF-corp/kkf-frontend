import { Typography } from "@mui/material";

const Settings = () => (
  <>
    <Typography variant="h4" gutterBottom>Ustawienia</Typography>
    <Typography>Wkrótce dostępne...</Typography>
  </>
);

export default Settings;
